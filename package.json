{
  "name": "fast-topics",
  "version": "1.0.0",
  "scripts": {
    "build:ts": "tsc",
    "build:go": "cd go && go build -o ../dist/topics.wasm",
    "build": "npm run build:ts && npm run build:go",
    "demo": "echo 'Visit localhost:8080/demo' && npx http-server"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/golang-wasm-exec": "^1.15.0",
    "typescript": "^4.5.5"
  }
}
